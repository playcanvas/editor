{
  "name": "@playcanvas/editor-api",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "chai": "^4.4.1",
    "karma": "^6.4.2",
    "karma-chrome-launcher": "^3.2.0",
    "karma-mocha": "2.0.1",
    "karma-spec-reporter": "^0.0.36",
    "mocha": "^10.2.0",
    "sinon": "^17.0.1"
  },
  "scripts": {
    "postinstall": "rollup -c",
    "build": "rollup -c --environment TEST:true",
    "test": "npm -s run build && karma start test/karma.conf.cjs --single-run",
    "test:debug": "npm -s run build && karma start test/karma.conf.cjs --single-run=false --browsers Chrome"
  }
}
